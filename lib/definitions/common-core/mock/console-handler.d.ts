export default class ConsoleHandler {
    private _oldMethods;
    private _mute;
    private _unmute;
    mute(method: string | string[]): void;
    unmute(method: string | string[]): void;
}
