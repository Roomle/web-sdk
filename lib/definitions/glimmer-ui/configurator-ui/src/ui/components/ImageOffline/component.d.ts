import Component from '@glimmer/component';
export default class ImageOffline extends Component {
    private _uiCore;
    noContent: boolean;
    cssStyle: string;
    private _hasError;
    didInsertElement(): void;
}
