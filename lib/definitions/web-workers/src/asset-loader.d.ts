export declare const enum WORKER_CONSTANTS {
    PREFIX = "rml",
    SEPERATOR = "."
}
export declare const enum WORKER_MESSAGE {
    PREFIX = 0,
    INIT = 1,
    ERROR = 2,
    ASSET_LOADED = 3,
    INIT_DONE = 4,
    LOAD_ASSET = 5
}
